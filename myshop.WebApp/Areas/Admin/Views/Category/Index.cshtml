@model IEnumerable<Category>
@{
    ViewBag.PageTitle = "Category Management";
    ViewBag.CardTitle = "Categories List";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<div class="container">
    <div class="row mb-3">
        <div class="col-md-4 col-12">
            <a class="btn btn-outline-primary btn-sm py-1 px-4 mt-0 animate__animated animate__fadeInUp" style="width: 100%;" asp-controller="Category" asp-action="Create">Add Category</a>
        </div>
        <div class="col-md-8 col-12">
            <form class="form-inline my-2 my-lg-0" asp-area="Admin" asp-action="Search" asp-controller="Category">
                <div class="input-group" style="width: 100%;">
                    <input class="form-control mr-sm-2" type="search" placeholder="Search" name="category" style="flex: 1;">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-outline-primary py-1 px-4 mt-0 animate__animated animate__fadeInUp">Search</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                    <th scope="col">Time</th>
                    <th scope="col">Event</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@Html.Raw(item.Description)</td>
                        <td>@item.CreatedTime.ToString("yyyy/MM/dd (hh:mm:ss)")</td>
                        <td>
                            <a href="/Admin/Category/Edit/@item.Id" class="btn btn-outline-success btn-sm">Edit</a>
                            <a href="/Admin/Category/Delete/@item.Id" class="btn btn-outline-danger btn-sm">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
